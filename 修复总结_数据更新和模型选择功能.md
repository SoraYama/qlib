# 修复总结：数据更新和模型选择功能

## 📅 修复时间
2025-10-23

## 🎯 修复的问题

### 问题1：更新价格数据不能同步到昨天的数据

**问题描述**：
- 点击"更新价格数据"按钮后，数据只能更新到硬编码的日期 `2025-10-12`
- 无法自动获取最新的数据（昨天的数据）

**根本原因**：
在 `/root/repos/qlib/web-ui/backend/services/qlib_service.py` 中，数据更新的日期范围是硬编码的：
```python
"--start", "2024-01-01",
"--end", "2025-10-12"  # 硬编码的固定日期
```

**修复方案**：
修改了以下三个数据更新方法，使其使用动态日期：
1. `update_price_data()` - 价格数据更新
2. `update_onchain_data()` - 链上数据更新
3. `update_news_data()` - 新闻数据更新

修复后的代码：
```python
from datetime import datetime, timedelta

# 动态计算日期范围：从2024-01-01到昨天
today = datetime.now()
yesterday = today - timedelta(days=1)
end_date = yesterday.strftime("%Y-%m-%d")

# 运行数据采集器
cmd = [
    "python",
    str(self.custom_scripts_dir / "gate_collector.py"),
    "download_data",
    "--source_dir", str(self.custom_scripts_dir.parent / "data" / "gate" / "source"),
    "--start", "2024-01-01",
    "--end", end_date  # 使用动态计算的日期
]

logger.info(f"Updating price data from 2024-01-01 to {end_date}")
```

**修复效果**：
- ✅ 数据更新现在会自动同步到昨天的日期
- ✅ 返回信息中包含实际更新到的日期
- ✅ 日志中记录了更新的日期范围
- ✅ 无需每次修改代码更新日期

---

### 问题2：实盘交易使用的模型和策略不明确

**问题描述**：
1. 实盘交易时，用户不知道正在使用什么模型
2. 没有选项让用户选择使用哪个模型
3. 交易状态中没有显示当前使用的模型信息
4. 默认使用 `lgb` 模型，但这是隐藏在代码中的

**根本原因**：
- 后端 `gate_service.py` 没有跟踪当前使用的模型
- 前端 `TradingPanel.tsx` 没有提供模型选择界面
- 交易状态 API 没有返回模型信息

**修复方案**：

#### 1. 后端修改 (`gate_service.py`)

添加了模型跟踪：
```python
# 初始化时添加 current_model 属性
self.current_model = 'lgb'  # 默认使用 LightGBM 模型

# start_trading 方法中接收 model_name 参数
self.current_model = config.get('model_name', 'lgb')

# get_trading_status 方法中返回当前模型
"current_model": getattr(self, 'current_model', 'lgb'),
```

#### 2. 后端路由修改 (`trading.py`)

更新了 `/trading/start` 接口的默认配置：
```python
default_config = {
    "model_name": "lgb",
    "strategy_name": "default",
    "initial_capital": 10000,
    "max_position_size": 0.3,
    "stop_loss": 0.05,
    "max_drawdown": 0.2
}
```

#### 3. 前端修改 (`TradingPanel.tsx`)

**新增功能**：
1. **模型选择下拉框**：
```tsx
<Select
  value={selectedModel}
  onChange={setSelectedModel}
  disabled={status?.is_running}
  style={{ width: 200 }}
  options={[
    { label: 'LightGBM', value: 'lgb' },
    { label: 'XGBoost', value: 'xgb' },
    { label: 'CatBoost', value: 'catboost' },
    { label: 'LSTM', value: 'lstm' },
    { label: 'Transformer', value: 'transformer' }
  ]}
/>
```

2. **模型信息展示卡片**：
```tsx
<Card>
  <Statistic
    title="使用模型"
    value={status?.current_model?.toUpperCase() || 'LGB'}
    prefix={<RobotOutlined />}
    valueStyle={{ color: '#1890ff', fontSize: '20px' }}
  />
  <div style={{ marginTop: 8, fontSize: 12, color: '#8c8c8c' }}>
    策略: {status?.current_strategy || 'default'}
  </div>
</Card>
```

3. **交易状态提示**：
```tsx
{status?.is_running && (
  <Alert
    message={`当前正在使用 ${status.current_model?.toUpperCase()} 模型进行实盘交易`}
    type="info"
    showIcon
    style={{ marginTop: 16 }}
  />
)}
```

#### 4. 前端状态管理修改 (`tradingSlice.ts`)

更新了 `startTrading` action 的类型定义：
```typescript
async (config: {
  model_name?: string;  // 新增 model_name 参数
  strategy_name: string;
  risk_limits: any
}) => {
  const response = await api.post('/trading/start', { config })
  return response.data.data
}
```

**修复效果**：
- ✅ 用户可以在启动交易前选择模型（LightGBM、XGBoost、CatBoost、LSTM、Transformer）
- ✅ 交易状态卡片中实时显示当前使用的模型
- ✅ 显示当前使用的策略名称
- ✅ 交易运行时有明确的提示信息显示正在使用的模型
- ✅ 交易运行时模型选择下拉框自动禁用，防止误操作

---

## 📊 可用的模型列表

系统现在支持以下5种模型：

| 模型名称 | 配置文件 | 适用场景 |
|---------|---------|---------|
| **LightGBM** | `lgb_config.yaml` | 默认模型，速度快，适合大多数场景 |
| **XGBoost** | `xgb_config.yaml` | 性能稳定，适合结构化数据 |
| **CatBoost** | `catboost_config.yaml` | 对类别特征处理好，无需过多特征工程 |
| **LSTM** | `lstm_config.yaml` | 适合时序数据，捕捉长期依赖 |
| **Transformer** | `transformer_config.yaml` | 最新架构，适合复杂模式识别 |

所有模型配置文件位于：`/root/repos/qlib/custom-scripts/model_configs/`

---

## 🎨 界面改进

### 交易控制面板

**改进前**：
- 只有"启动交易"和"停止交易"按钮
- 看不到使用的模型信息

**改进后**：
- 新增"选择模型"下拉框
- 新增"使用模型"状态卡片
- 实时显示当前模型和策略
- 交易运行时显示提示信息

### 交易状态卡片布局

原来的3列布局改为4列：
1. **交易状态** - 运行中/已停止
2. **使用模型** - 当前模型名称和策略 ⭐ 新增
3. **总资产** - 账户总余额
4. **今日盈亏** - 当日损益情况

---

## 🔍 使用说明

### 如何选择和查看模型

1. **启动交易前选择模型**：
   - 在"交易控制"面板中，找到"选择模型"下拉框
   - 从5个可用模型中选择一个
   - 点击"启动交易"按钮

2. **查看当前使用的模型**：
   - 查看顶部状态卡片中的"使用模型"
   - 或查看交易控制面板下方的蓝色提示信息

3. **更新数据到最新**：
   - 进入"数据管理"页面
   - 点击"更新价格数据"、"更新链上数据"或"更新新闻数据"
   - 系统会自动更新到昨天的日期
   - 查看返回信息中的实际更新日期

---

## 📝 技术细节

### 修改的文件列表

**后端文件**：
1. `/root/repos/qlib/web-ui/backend/services/qlib_service.py`
   - 修改了 `update_price_data()` 方法
   - 修改了 `update_onchain_data()` 方法
   - 修改了 `update_news_data()` 方法

2. `/root/repos/qlib/web-ui/backend/services/gate_service.py`
   - 添加了 `current_model` 属性
   - 修改了 `start_trading()` 方法
   - 修改了 `get_trading_status()` 方法

3. `/root/repos/qlib/web-ui/backend/routes/trading.py`
   - 修改了 `/trading/start` 路由的默认配置

**前端文件**：
1. `/root/repos/qlib/web-ui/frontend/src/pages/TradingPanel.tsx`
   - 新增了模型选择下拉框
   - 新增了模型信息展示卡片
   - 新增了交易状态提示
   - 调整了状态卡片布局

2. `/root/repos/qlib/web-ui/frontend/src/store/slices/tradingSlice.ts`
   - 更新了 `startTrading` action 的类型定义

---

## ✅ 测试建议

### 测试数据更新功能

1. 进入数据管理页面
2. 点击"更新价格数据"
3. 检查返回消息中是否包含昨天的日期
4. 检查后端日志，确认使用了动态日期

### 测试模型选择功能

1. 进入实盘交易页面
2. 确认看到"选择模型"下拉框
3. 选择不同的模型（如 XGBoost）
4. 点击"启动交易"
5. 确认状态卡片显示选择的模型
6. 确认看到蓝色提示信息
7. 停止交易后，尝试选择其他模型

---

## 🚀 后续优化建议

1. **模型性能对比**：
   - 添加不同模型的历史表现对比
   - 显示各模型的胜率、收益率等指标

2. **模型热切换**：
   - 支持在交易运行时切换模型
   - 平滑过渡，不影响已有持仓

3. **模型自动选择**：
   - 根据市场状态自动选择最优模型
   - 基于回测结果动态调整

4. **数据更新优化**：
   - 添加增量更新功能
   - 支持指定日期范围更新
   - 添加数据更新进度显示

---

## 📞 问题反馈

如果在使用过程中遇到问题，请检查：

1. **模型未生效**：
   - 确认在启动交易前选择了正确的模型
   - 检查后端日志中的模型名称

2. **数据更新失败**：
   - 检查 Gate.io API 是否可访问
   - 查看后端日志中的错误信息
   - 确认数据采集脚本是否存在

3. **界面显示异常**：
   - 刷新浏览器
   - 清除缓存
   - 检查浏览器控制台错误

---

## 🎉 总结

本次修复解决了两个重要的用户体验问题：

1. **数据更新时效性** - 现在可以自动获取到最新的数据
2. **模型透明度** - 用户可以清楚地知道和选择使用的模型

这些改进让系统更加用户友好，提升了实盘交易的可控性和透明度。

